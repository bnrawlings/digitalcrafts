const intialState = {
    firstName: "",
    lastName: "",
    email: "",
    password: "",
};

const SignUpFormData = (state = intialState, action) => {
    switch(action.type){
        default:
            return state
    }
}
export default SignUpFormData;